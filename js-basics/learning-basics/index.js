// comments
let new_variable = "Rays Variable";
console.log(new_variable);

let person = {
    name: 'Ray', age: 18, education: "CalPoly"
};

console.log(person);

let first_list = ['strings', 5, ['list']];
console.log(first_list);

function say_hi(name) {
    console.log('Hello ' + name);
}

say_hi('Ray');

function square(num) {
    return (num * num);
}

console.log(square(15))
